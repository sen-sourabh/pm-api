export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum HttpStatusViaCode {
  Info = 'info',
  Success = 'success',
  Redirect = 'redirect',
  Client_Error = 'client_error',
  Server_Error = 'server_error',
}
